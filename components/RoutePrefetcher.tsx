"use client";

/**
 * RoutePrefetcher disabled — Next.js auto-prefetches visible <Link> elements.
 * Manual prefetching was triggering 3 extra Vercel function invocations per page load.
 */
export default function RoutePrefetcher() {
  return null;
}
